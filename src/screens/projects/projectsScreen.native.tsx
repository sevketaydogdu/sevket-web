import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const ProjectsScreenNative = () => {
  return (
    <SafeAreaView>
      <Text>ProjectsScreenNative</Text>
    </SafeAreaView>
  );
};

export default ProjectsScreenNative;
